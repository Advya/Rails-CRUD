<h1>Hello, Rails!</h1>
<p>There are <%= Article.public_count %> articles and counting!</p>
<p>List of all articles below</p>

<ul>
    <% @articles.each do |article| %>
        <% unless article.archived? %>
            <li>            
                <%= link_to article.title, article %>
            </li>
        <% end %>
    <% end %>
</ul>

<%= link_to "New Article", new_article_path %>